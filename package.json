{
  "name": "117-site",
  "version": "1.0.0",
  "description": "Static site with code quality gates and Lighthouse CI",
  "scripts": {
    "start": "ELEVENTY_ENV=development eleventy --serve",
    "build": "eleventy && node scripts/optimize-build.js",
    "build:dev": "eleventy",
    "dev": "ELEVENTY_ENV=development eleventy --serve",
    "clean": "rm -rf _site",
    "optimize": "node scripts/optimize-build.js",
    "lint": "npm run lint:js && npm run lint:css && npm run lint:md && npm run lint:format && npm run lint:duplication",
    "lint:js": "eslint \"src/**/*.js\"",
    "lint:css": "stylelint \"src/**/*.css\"",
    "lint:md": "markdownlint \"**/*.md\" --ignore node_modules --ignore _site --ignore references --ignore qa_agents --ignore archive --ignore private_strategy --config .markdownlint.yaml",
    "lint:format": "prettier --check \"**/*.{js,css,md,html,json}\" --ignore-path .gitignore",
    "lint:duplication": "jscpd src/",
    "lint:links": "npm run lint:links:md && npm run lint:links:html",
    "lint:links:md": "markdown-link-check docs/**/*.md README.md --config .markdown-link-check.json --quiet",
    "lint:links:html": "npm run build && linkinator _site --recurse --skip '^(?!http://localhost|https://kaw393939.github.io)' --markdown",
    "lint:links:staged": "node scripts/check-staged-links.js",
    "format": "prettier --write \"**/*.{js,css,md,html,json}\" --ignore-path .gitignore",
    "lighthouse": "lhci autorun",
    "audit:pages": "node scripts/audit-all-pages.js",
    "ci:local": "node scripts/run-local-ci.js",
    "ci:fast": "node scripts/run-local-ci.js --fast",
    "migrate:paths": "node scripts/migrate-paths.js",
    "validate:paths": "node scripts/validate-paths.js",
    "prepare": "husky install",
    "test": "npm run lint && npm run build && npm run audit:pages && npm run lint:links"
  },
  "keywords": [
    "static-site",
    "eleventy",
    "github-pages"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1",
    "@lhci/cli": "^0.13.0",
    "@quasibit/eleventy-plugin-sitemap": "^2.2.0",
    "eslint": "^8.57.0",
    "eslint-config-prettier": "^9.1.0",
    "husky": "^8.0.3",
    "jscpd": "^4.0.5",
    "jsdom": "^27.0.1",
    "linkinator": "^7.3.0",
    "lint-staged": "^15.2.0",
    "markdown-link-check": "^3.14.1",
    "markdownlint-cli": "^0.39.0",
    "prettier": "^3.2.5",
    "stylelint": "^16.2.1",
    "stylelint-config-standard": "^36.0.0",
    "stylelint-prettier": "^5.0.0"
  }
}
