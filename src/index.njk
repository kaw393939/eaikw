---
layout: base.njk
title: Home
description: Welcome to my professional portfolio
---

<section class="hero">
    <div class="hero-content">
        <h1 class="hero-title">Hi, I'm <span class="highlight">{{ site.author }}</span></h1>
        <p class="hero-subtitle">Full Stack Developer & Designer</p>
        <p class="hero-description">
            I create modern, responsive web applications and digital experiences. 
            Passionate about clean code, user experience, and innovative solutions.
        </p>
        <div class="hero-actions">
            <a href="/projects/" class="btn btn-primary">View My Work</a>
            <a href="/about/" class="btn btn-secondary">About Me</a>
        </div>
    </div>
</section>

<section class="featured-projects">
    <div class="container">
        <h2 class="section-title">Featured Projects</h2>
        <div class="projects-grid">
            {% for project in collections.projects | limit(3) %}
            <article class="project-card">
                {% if project.data.image %}
                <div class="project-image">
                    <img src="{{ project.data.image }}" alt="{{ project.data.title }}" loading="lazy">
                </div>
                {% endif %}
                <div class="project-info">
                    <h3 class="project-title">
                        <a href="{{ project.url }}">{{ project.data.title }}</a>
                    </h3>
                    <p class="project-description">{{ project.data.description }}</p>
                    {% if project.data.technologies %}
                    <div class="project-tech">
                        {% for tech in project.data.technologies | limit(3) %}
                        <span class="tech-tag">{{ tech }}</span>
                        {% endfor %}
                    </div>
                    {% endif %}
                </div>
            </article>
            {% endfor %}
        </div>
        <div class="section-footer">
            <a href="/projects/" class="btn btn-outline">View All Projects</a>
        </div>
    </div>
</section>

<section class="recent-posts">
    <div class="container">
        <h2 class="section-title">Latest Blog Posts</h2>
        <div class="posts-grid">
            {% for post in collections.blog | limit(3) %}
            <article class="post-card">
                <div class="post-info">
                    <h3 class="post-title">
                        <a href="{{ post.url }}">{{ post.data.title }}</a>
                    </h3>
                    <p class="post-excerpt">{{ post.templateContent | excerpt }}</p>
                    <div class="post-meta">
                        <time class="post-date">{{ post.data.date | dateFormat }}</time>
                        {% if post.data.tags %}
                        <div class="post-tags">
                            {% for tag in post.data.tags %}
                                {% if tag != "blog" %}
                                <span class="tag">{{ tag }}</span>
                                {% endif %}
                            {% endfor %}
                        </div>
                        {% endif %}
                    </div>
                </div>
            </article>
            {% endfor %}
        </div>
        <div class="section-footer">
            <a href="/blog/" class="btn btn-outline">View All Posts</a>
        </div>
    </div>
</section>